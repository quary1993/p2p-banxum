{
  "corrections": [
    {
      "location": "Flow 'Onboard a borrower and publish their first loan' > preconditions (jargon): \"'KYB status' values are pending, manual_review, approved, rejected.\"",
      "problem": "Wrong and incomplete status enum. The code has no 'rejected' value; the negative outcome is 'declined'. The full set in entities/models.py BorrowerKybStatus is: not_started, pending, approved, declined, manual_review, expired, reverification_required. A newcomer told the value is 'rejected' will never find it in the Edit dropdown.",
      "fix": "State the real values: not_started, pending, approved, declined, manual_review, expired, reverification_required. The blocking 'no' value is 'declined' (not 'rejected'). Only 'approved' (with compliance hold off) lets a loan publish.",
      "severity": "high"
    },
    {
      "location": "Flow 'Record a borrower repayment...' > step 2: \"WARNING GATE: if the amount is LESS than the expected installment due, the backend requires the 'Warning acknowledged...' checkbox\" and pitfall 1.",
      "problem": "HISTORICAL (pre-refinancing slice): at the time, the warning-acknowledged gate fired whenever the amount differed from the next-due installment in either direction, and an overpayment was only allowed on a 'funded' loan. That mechanism has since been removed entirely.",
      "fix": "SUPERSEDED in the refinancing-loans slice: the warning-acknowledged mechanism was removed entirely. A regular installment now uses a FIXED amount equal to the outstanding amount of the next due installment (not editable); any other amount (except defaulted loans, which go through recovery) is declared as a 'Repayment in advance' with a 'Borrower repayment bank date' and a mandatory preview/confirmation dialog (POST /api/v1/servicing/admin/borrower-repayments/advance-preview/). The flow text now documents that model.",
      "severity": "high"
    },
    {
      "location": "Flow 'Disburse a funded loan to the borrower' > step 3 and step 4 (GATES).",
      "problem": "HISTORICAL (pre-refinancing slice): at the time, finalize_borrower_disbursement required the entered Amount to equal the loan's outstanding borrower-disbursement-payable balance exactly, plus sufficient collection cash. The equality rule has since been replaced by the default funded-principal-minus-fee model with an explained joint override.",
      "fix": "SUPERSEDED in the refinancing-loans slice: 'Close funding' now moves the FULL accepted principal into the borrower payable (no fee accrual at close; planned fee recorded as evidence only). The disbursement defaults to funded principal minus the BANXUM fee (default 2% via borrower_success_fee_bps), the fee books as Garanta revenue at disbursement, amount and fee are readonly and can only be overwritten together with a required explanation note, validations are transfer + fee <= funded amount and fee between 0 and 10% of funded, collection cash must cover the transfer, and any unpaid remainder stays in the payable. The flow text now documents that model.",
      "severity": "high"
    },
    {
      "location": "Flow 'Handle a day-60 forced withdrawal' > preconditions (jargon 'Penalty mode'), step 3, outcome, and pitfall 2.",
      "problem": "Understated. When there is no verified payout instruction, the over-age lot is not merely 'flagged' in penalty mode - the scan moves the lot to PENALTY_MODE AND charges a real per-day penalty against the lot's available balance (ledger/services.py _charge_balance_penalty_for_lot, BALANCE_PENALTY_BPS_PER_DAY), recording a journal entry each time the scan runs while still overdue. Calling it just a flag for follow-up hides that the investor's balance is actively being reduced.",
      "fix": "Add that penalty mode actively charges a daily penalty (rate BALANCE_PENALTY_BPS_PER_DAY) on the over-age amount every time the scan runs while no verified IBAN exists - so registering a verified IBAN and re-running is time-sensitive, not just bookkeeping.",
      "severity": "medium"
    },
    {
      "location": "Flow 'Match an incoming bank deposit to a lender' > preconditions (jargon 'balance lot') and Flow 'day-60' pitfall 3 (\"The 60-day clock is per-lot... a new deposit does not reset an older lot\").",
      "problem": "Slightly inaccurate on when the clock starts. The lot's investment_deadline (30 days) and withdrawal_deadline (60 days) are computed from received_at, which is derived from the VALUE DATE you enter on the deposit (_received_at_from_value_date / _lot_deadlines in ledger/services.py), not literally 'the day it arrived' or the day you keyed it. An admin who back- or forward-dates the value date shifts the whole ageing clock.",
      "fix": "Note that the 30/60-day ageing clock runs from the deposit's VALUE DATE (the value_date field), so entering the value date correctly matters - it sets when reminders fire and when the lot becomes over-age.",
      "severity": "medium"
    },
    {
      "location": "Flow 'Match an incoming bank deposit to a lender' > step 4: \"Enter Amount (in minor units), Currency, Booking date, Value date, and confirm the Collection account.\"",
      "problem": "Field-label mismatch. On the Lender deposit card the amount field is labelled 'Amount minor units' (not 'Amount'), and Payer name + Payment reference sit in a separate row from Amount/Currency/Booking date/Value date/Collection account. Minor, but the manual should name the on-screen label exactly.",
      "fix": "Refer to the field as 'Amount minor units'. The card title is 'Lender deposit'.",
      "severity": "low"
    },
    {
      "location": "Flow 'Match an incoming bank deposit to a lender' > step 3 (investor lookup matches \"... or a matching payout IBAN\").",
      "problem": "Partially misleading in context. The backend investor lookup CAN match on a payout IBAN, but only when the IBAN is passed to it - the Lender deposit card's 'Investor from bank reference' input does NOT pass an IBAN (only the Payout instruction card's lookup does). So on the deposit card you cannot actually find an investor by typing an IBAN; IBAN matching is available on the Payout instruction card.",
      "fix": "Clarify that on the Lender deposit card the lookup matches reference, full name, email, and UUID; IBAN matching is only wired into the Payout instruction card's investor lookup.",
      "severity": "low"
    },
    {
      "location": "Flow 'Onboard a borrower and publish their first loan' > step 4: \"confirm it via the Borrower lookup\" / create-loan field list.",
      "problem": "The 'Create loan draft' modal does not pre-fill or lock the borrower to the row you selected in the Borrowers table - it shows a 'Selected borrower' context bar but the form contains an editable Borrower lookup you must set/confirm, and the modal field set does not include Collateral description, Purpose description, Interest-only months, or the success/recovery fee fields (those exist only on the backend command with defaults). The step's phrase 'The selected borrower is pre-filled' overstates the binding.",
      "fix": "Say the modal shows the selected borrower as context but you confirm/choose the borrower in the Borrower lookup inside the form. Updated in the refinancing-loans slice: the visible create fields are Borrower, Title, Financeable principal minor units, Currency, Interest bps, Term months, Purpose, Repayment type, Collateral type, Collateral value minor, Risk rating, Loan start date (defaults to the funding deadline), Funding deadline, Investor summary, and the 'Refinancing loan' checkbox with its original loan data fields. There is no First installment pay date field; the first installment is derived as one month after the loan start date.",
      "severity": "low"
    }
  ],
  "confirmedAccurate": [
    "Loan create validations (updated in the refinancing-loans slice): financeable principal 1,000-1,000,000,000 in loan currency, term 1-600 months, funding deadline not in past and <=60 days out, schedule complete and summing to the financeable principal, refinancing loans require the four original loan data fields; the first installment is derived as one month after the loan start date (loans/services.py).",
    "Publish gates (updated in the refinancing-loans slice): status must be DRAFT; borrower.can_transact (KYB approved AND compliance hold off); publishable funding deadline must be today..+29 days (MAX_PUBLISHABLE_FUNDING_DEADLINE_DAYS=29, stricter than the 60-day create limit); schedule complete and matching the financeable principal; refinancing loans must not have a financeable principal above the remaining outstanding of the informational original schedule (lower is allowed with an informational notice).",
    "Publish failure string 'Borrower KYB must be approved and free of compliance hold.' is exact, and the 29-day publish rationale (late-pledged funds still settle inside the 60-day operating limit) matches the code comment.",
    "A KYB status change requires a note OR evidence summary ('A note or evidence summary is required for KYB changes.' in entities/services.py update_borrower_entity).",
    "Borrowers cannot be deleted (no delete path; UnsupportedRemoveNote 'Borrower' in the UI; records kept via append-only events).",
    "Balance ageing reminder days are exactly 25, 46, 53, 58, 59, 60 (BALANCE_AGEING_REMINDER_DAYS).",
    "Forced-withdrawal logic: per overdue investor/currency the scan looks for an active VERIFIED payout instruction; if present it creates a withdrawal request (status 'requested'); if absent the lot goes to penalty mode and no withdrawal is created.",
    "Dry-run vs live ageing scan, optional As-of timestamp and Currency filter inputs, and the requirement to register a verified IBAN then re-run are accurate.",
    "Deposit gates: currency enabled, positive amount, eligible lender, and required source IBAN passing country-length and MOD-97 checksum validation. On success the reconciled BankOperation, journal entry, balance lot, and exact source-IBAN verified payout instruction are committed atomically.",
    "KYC manual-review decision options Approve/Decline/Request Reverification/Reopen and their targets (Approved / Declined / Reverification Required / back to Manual Review) match _manual_review_target_status; Approve activates a pending_kyc account.",
    "KYC non-overridable approval gate: cannot manually Approve a case whose status is 'sanctions_hit' or 'declined' (NON_OVERRIDABLE_APPROVAL_STATUSES); Approve only allowed from review-routed statuses manual_review/high_risk/pep_hit/adverse_media_hit (MANUAL_REVIEW_STATUSES).",
    "KYC decision requires at least one of Officer note or Evidence summary ('A note or evidence summary is required.').",
    "Servicing thresholds: 'late' at >=5 days past due (LATE_THRESHOLD_DAYS), 'defaulted' at >=16 days (DEFAULT_THRESHOLD_DAYS); status scan only evaluates funded/late loans; repayment allowed only on funded/late (defaulted routed to recovery); recovery allowed only on defaulted (RECOVERY_ALLOWED_LOAN_STATUSES).",
    "Servicing UI (updated in the refinancing-loans slice): repayments and recoveries are recorded from the loan row's Manage dialog ('Record borrower repayment' with the fixed next-due amount or the 'Repayment in advance' checkbox plus 'Borrower repayment bank date' and preview dialog; 'Record a recovery payment' for defaulted loans); the Servicing operations card keeps Run status scan and Publish public note (Risk note body). The 'Warning acknowledged for partial/irregular borrower repayment' checkbox no longer exists.",
    "Secondary-market: performing (funded) listings auto-publish to ACTIVE; late/defaulted listings land in APPROVAL_REQUESTED. Approve and Reject both require APPROVAL_REQUESTED; Remove works on ACTIVE or APPROVAL_REQUESTED. Each records reason/audit. Disclosure note required to approve.",
    "Payout instruction: investor must be a lender account, currency enabled, and IBAN must pass country-length plus MOD-97 checksum validation. Distinct IBANs are additive; verification updates the exact instruction without disabling other verified accounts. Investor self-service requests create a linked payout-verification finance task; admin verification resolves it. The IBAN-collision warning remains when iban_match_count > 1.",
    "Withdrawal execution: the 'Requested withdrawal' lookup is filtered to status=requested; finalize only works on REQUESTED (re-finalizing a FINALIZED one returns the existing result); cancel only works on REQUESTED and restores the lots via the stored allocations.",
    "Borrower disbursement (updated in the refinancing-loans slice): loan must be 'funded' ('Loan must be funded before borrower disbursement.'), loan currency must match disbursement currency, borrower must match the loan ('Borrower does not match loan.'), borrower must be able to transact; selecting a loan auto-fills and locks the read-only Borrower field and the Currency; the default amount is funded principal minus the BANXUM fee (default 2%), amount and fee are readonly unless overwritten together with a required explanation note, transfer + fee <= funded amount, fee between 0 and 10% of funded, the fee books as Garanta revenue at disbursement, and the action is also offered in the Loans Manage dialog for funded loans.",
    "Reports: report type / output format (csv/pdf/zip) / redaction mode (redacted/full) / period preset (custom uses start+end, rolling presets use end as anchor and ignore start); artifact panel shows filename, SHA-256 checksum, generated timestamp, encoding, content preview and manifest; base64 PDF/ZIP must be saved via Download artifact.",
    "Daily dashboard KPI tiles (Open queue items, High priority + urgent, Overdue SLA, Finance actions) and the 13 queue tabs (Admin tasks, KYC reviews, Bank exceptions, Withdrawals, Forced withdrawals, Balance ageing, Funding loans, Servicing due, Loan risk, Secondary approvals, FX settlement, Failed emails, Reconciliation breaks); 'Finance actions' sums bank_operations_pending + withdrawals_requested + forced_withdrawals_requested + fx_settlement_deltas + reconciliation_breaks; 'Create tasks' on the Reconciliation breaks tab syncs break snapshots into tasks reporting created vs existing counts.",
    "Admin/account management: Create admin (Email, Full name, Initial password) with no forgot-password flow; Account access controls statuses Active/Restricted/Locked/Closed with reason code + note/evidence; clean-account closure checkbox is an admin attestation; users cannot be deleted (UnsupportedRemoveNote 'User')."
  ]
}
